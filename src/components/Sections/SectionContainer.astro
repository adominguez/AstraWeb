---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class:className = '', ...rest } = Astro.props;
---

<section class:list={['flex flex-col justify-end max-w-5xl w-full', className]} {...rest}>
  <slot />
</section>